/**
 * The two hats the player wears. Chairman is club-level executive control;
 * FA President is the future-ready association-level role. Both are the
 * player — the Manager is never a role the player plays (see
 * docs/ARCHITECTURE.md section 0).
 */
export type Role = 'chairman' | 'fa_president';
